services:
  - type: web
    name: restaurant-management
    env: php
    plan: free
    buildCommand: composer install
    startCommand: php -S 0.0.0.0:$PORT -t .
    envVars:
      - key: PORT
        sync: false